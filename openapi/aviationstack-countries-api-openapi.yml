openapi: 3.1.0
info:
  title: AviationStack Aircraft Types Countries API
  version: 1.0.0
  description: Real-time and historical flight tracking, airline routes, airports, airlines, aircraft, taxes, cities, countries, and timetable data via REST endpoints.
servers:
- url: https://aviationstack.com/
tags:
- name: Countries
paths:
  /v1/countries:
    get:
      summary: Country information
      responses:
        '200':
          description: Countries collection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Collection'
      tags:
      - Countries
components:
  schemas:
    Collection:
      type: object
      properties:
        pagination:
          type: object
          additionalProperties: true
        data:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: true
x-generated-from: https://aviationstack.com/
x-generated-by: claude-crawl-2026-05-08
