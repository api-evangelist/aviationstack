openapi: 3.1.0
info:
  title: AviationStack Aircraft Types Airlines API
  version: 1.0.0
  description: Real-time and historical flight tracking, airline routes, airports, airlines, aircraft, taxes, cities, countries, and timetable data via REST endpoints.
servers:
- url: https://aviationstack.com/
tags:
- name: Airlines
paths:
  /v1/airlines:
    get:
      summary: Airline data
      responses:
        '200':
          description: Airlines collection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Collection'
      tags:
      - Airlines
components:
  schemas:
    Collection:
      type: object
      properties:
        pagination:
          type: object
          additionalProperties: true
        data:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: true
x-generated-from: https://aviationstack.com/
x-generated-by: claude-crawl-2026-05-08
